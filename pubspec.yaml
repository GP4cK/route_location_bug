name: route_location_bug
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.19.4 <3.0.0"

dependencies:
  go_router: ^7.1.1
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.0.0
  go_router_builder: ^2.0.2
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
